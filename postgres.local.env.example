# Copy to .env.local and replace both placeholders with long random hex values.
# Keep .env.local private; it is ignored by Git.
OPENWEBUI_POSTGRES_PASSWORD=REPLACE_WITH_RANDOM_HEX
WEBUI_SECRET_KEY=REPLACE_WITH_ANOTHER_RANDOM_HEX
